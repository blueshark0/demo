package com.learn.m05_methods;

/*
 * ============================================================
 * 模块：m05 方法
 * 关卡：Ex05 可变参数
 * ------------------------------------------------------------
 * 【知识点】
 *   可变参数让方法可以接收“任意个数”的同类型参数，写法是 类型...：
 *       static int sum(int... nums) { ... }
 *   在方法内部，nums 就是一个普通数组，可以用 for-each 遍历。
 *   调用时可以传 0 个、1 个或很多个： sum()、sum(1,2,3)、sum(1,2,3,4) 都行。
 *
 * 【本关任务】
 *   实现 sum(int... nums)，返回所有参数的和（没有参数时返回 0）。
 *
 * 【如何闯关】
 *   补全 sum 的方法体，运行对照输出。
 * ============================================================
 */
public class Ex05_Varargs {

    static int sum(int... nums) {
        // TODO(关卡5-1)：遍历 nums 求和并返回
        //   提示：先 int total = 0; 再 for (int n : nums) total += n; 最后 return total;
        throw new UnsupportedOperationException("请补全 sum() 方法体");
    }

    public static void main(String[] args) {
        System.out.println("sum() = " + sum());
        System.out.println("sum(1, 2, 3) = " + sum(1, 2, 3));
        System.out.println("sum(10, 20, 30, 40) = " + sum(10, 20, 30, 40));
    }
}

/*
 * ============================ 预期输出 ============================
 * sum() = 0
 * sum(1, 2, 3) = 6
 * sum(10, 20, 30, 40) = 100
 * ===============================================================
 *
 * 排查提示：
 *  - 没传参数时 nums 是一个空数组，循环一次都不执行，total 保持 0。
 *  - 可变参数只能放在参数列表的最后一个位置。
 */
