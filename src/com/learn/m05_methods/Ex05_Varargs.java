package com.learn.m05_methods;

/*
 * ============================================================
 * 模块：m05 方法
 * 关卡：Ex05 可变参数
 * ------------------------------------------------------------
 * 【知识点】
 *   可变参数让方法可以接收“任意个数”的同类型参数，声明时在类型后面加
 *   三个点（类型...）。在方法内部，这个可变参数其实就是一个普通数组，
 *   可以像遍历数组一样把里面的元素一个个取出来处理。调用时可以传 0 个、
 *   1 个或很多个实参都行。注意：可变参数只能放在参数列表的最后一个位置。
 *
 * 【本关任务】
 *   实现 sum(int... nums)，返回所有参数的和（没有参数时返回 0）。
 *   main 会调用 sum()、sum(1, 2, 3)、sum(10, 20, 30, 40)，期望分别打印：
 *       sum() = 0
 *       sum(1, 2, 3) = 6
 *       sum(10, 20, 30, 40) = 100
 * ============================================================
 */
public class Ex05_Varargs {

    static int sum(int... nums) {
        // TODO(关卡5-1)：遍历 nums 把所有元素累加起来并返回（没有元素时结果为 0）
        throw new UnsupportedOperationException("请补全：返回 nums 中所有元素的和");
    }

    public static void main(String[] args) {
        System.out.println("sum() = " + sum());
        System.out.println("sum(1, 2, 3) = " + sum(1, 2, 3));
        System.out.println("sum(10, 20, 30, 40) = " + sum(10, 20, 30, 40));
    }
}

/*
 * ============================ 预期输出 ============================
 * sum() = 0
 * sum(1, 2, 3) = 6
 * sum(10, 20, 30, 40) = 100
 * ===============================================================
 *
 * 排查提示：
 *  - 运行时若抛出 UnsupportedOperationException，说明方法体还没补全。
 *  - 没传参数时 nums 是一个空数组，一个元素都没有，求和结果应为 0。
 *  - 可变参数只能放在参数列表的最后一个位置。
 */
