package com.learn.m05_methods;

/*
 * ============================================================
 * 模块：m05 方法
 * 关卡：Ex04 递归
 * ------------------------------------------------------------
 * 【知识点】
 *   递归 = 方法在自己内部又调用自己，把大问题拆成更小的同类问题。
 *   以阶乘为例：n! = n × (n-1) × ... × 1，可以写成 n! = n × (n-1)!
 *   递归必须有“终止条件”（也叫递归出口），否则会无限递归导致栈溢出：
 *       if (n <= 1) return 1;          // 出口
 *       return n * factorial(n - 1);   // 把问题缩小一层
 *
 * 【本关任务】
 *   用递归实现 factorial(n)，计算 n 的阶乘。
 *
 * 【如何闯关】
 *   补全 factorial 的方法体（含终止条件），运行对照输出。
 * ============================================================
 */
public class Ex04_Recursion {

    static long factorial(int n) {
        // TODO(关卡4-1)：用递归返回 n!
        //   提示：当 n <= 1 时返回 1；否则返回 n * factorial(n - 1)
        throw new UnsupportedOperationException("请补全 factorial() 方法体");
    }

    public static void main(String[] args) {
        System.out.println("5! = " + factorial(5));
    }
}

/*
 * ============================ 预期输出 ============================
 * 5! = 120
 * ===============================================================
 *
 * 排查提示：
 *  - 5! = 5×4×3×2×1 = 120。
 *  - 千万别忘了终止条件 if (n <= 1) return 1; 否则会一直递归到崩溃。
 *  - 用 long 而不是 int，是因为阶乘增长极快，int 容易装不下。
 */
