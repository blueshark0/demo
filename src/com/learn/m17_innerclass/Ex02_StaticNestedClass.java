package com.learn.m17_innerclass;

/*
 * ============================================================
 * 模块：m17 内部类与匿名类
 * 关卡：Ex02 静态嵌套类
 * ------------------------------------------------------------
 * 【知识点】
 *   静态嵌套类是写在另一个类内部、且带 static 的类。和成员内部类相反，它
 *   不依附于任何外部对象：你不需要先有外部对象，就能直接创建它。代价是它
 *   访问不到外部类的非静态字段（因为压根没有“所属的那个外部对象”）。
 *   静态嵌套类很适合用来表达“只服务于外部类的一个小数据结构”，把相关的
 *   小类型收纳在一起，又不增加对外部实例的依赖。
 *
 * 【本关任务】
 *   下方外部类 Geometry 里有一个静态嵌套类 Point，带坐标 x、y，和一个方法
 *   distanceToOrigin()，要返回该点到原点 (0,0) 的直线距离。
 *   1. 补全 distanceToOrigin()，返回到原点的距离，即根号(x*x + y*y)。
 *   2. 在 main 中创建一个坐标为 (3, 4) 的 Point，并打印
 *      「点(3, 4) 到原点距离 = 5.0」（3-4-5 勾股，结果恰为 5.0）。
 * ============================================================
 */
public class Ex02_StaticNestedClass {

    static class Geometry {
        static class Point {
            int x;
            int y;

            Point(int x, int y) {
                this.x = x;
                this.y = y;
            }

            double distanceToOrigin() {
                throw new UnsupportedOperationException("请补全：返回到原点的距离 = 根号(x*x + y*y)");
            }
        }
    }

    public static void main(String[] args) {
        // TODO(关卡2-1)：创建一个坐标为 (3, 4) 的 Point（静态嵌套类可直接创建，不需要外部对象）
        // TODO(关卡2-2)：打印「点(3, 4) 到原点距离 = 5.0」
    }
}

/*
 * ============================ 预期输出 ============================
 * 点(3, 4) 到原点距离 = 5.0
 * ===============================================================
 *
 * 排查提示：
 *  - 静态嵌套类不需要外部对象就能创建；如果你试图先创建 Geometry 对象再去
 *    创建 Point，说明还没体会到“静态”二字的含义。
 *  - 结果应是 double 的 5.0 而不是 5；求平方根的运算在 Math 里能找到。
 */
