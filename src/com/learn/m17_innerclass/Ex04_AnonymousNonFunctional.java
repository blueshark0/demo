package com.learn.m17_innerclass;

/*
 * ============================================================
 * 模块：m17 内部类与匿名类
 * 关卡：Ex04 匿名类实现抽象类
 * ------------------------------------------------------------
 * 【知识点】
 *   匿名类是“没有名字、就地写出、立刻 new 出对象”的类。它既能实现接口，也能
 *   继承（实现）一个抽象类——在 new 后面紧跟一对花括号，把要补的抽象方法写在
 *   里面即可，写完同时就得到了一个对象。它和 Lambda 的区别很关键：Lambda 只能
 *   用于“只有一个抽象方法”的函数式接口；而抽象类、或带多个抽象方法的类型，就
 *   只能用匿名类。匿名类还能直接用上父类里已经写好的方法。
 *
 * 【本关任务】
 *   下方抽象类 Shape 有抽象方法 area()，并已实现 report() 返回 "面积 = " + area()。
 *   请在 main 中用匿名类“就地”创建一个 Shape 对象，代表一个边长固定为 5 的正方形：
 *   在匿名类里重写 area() 返回 5*5。然后打印这个对象的 report()，
 *   得到一行「面积 = 25.0」（area() 返回 double，故为 25.0）。
 * ============================================================
 */
public class Ex04_AnonymousNonFunctional {

    abstract static class Shape {
        abstract double area();

        String report() {
            return "面积 = " + area();
        }
    }

    public static void main(String[] args) {
        // TODO(关卡4-1)：用匿名类就地创建一个 Shape 对象，重写 area() 返回 5*5（边长 5 的正方形）
        //   提示：匿名类里需要补全的 area() 方法体先用 throw 占位，由你替换为正确实现
        Shape square = new Shape() {
            @Override
            double area() {
                throw new UnsupportedOperationException("请补全：返回边长平方，即 5 * 5");
            }
        };

        // TODO(关卡4-2)：打印 square 的 report() 结果，得到「面积 = 25.0」
    }
}

/*
 * ============================ 预期输出 ============================
 * 面积 = 25.0
 * ===============================================================
 *
 * 排查提示：
 *  - 抽象类不能直接 new；只有把抽象方法 area() 在花括号里补全后，匿名类才算完整。
 *  - report() 是父类已经写好的方法，你不用重写它，直接调用即可。
 *  - 结果是 25.0 而不是 25，因为 area() 的返回类型是 double。
 */
