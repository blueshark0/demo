package com.learn.m17_innerclass;

/*
 * ============================================================
 * 模块：m17 内部类与匿名类
 * 关卡：Ex03 局部（方法内）内部类
 * ------------------------------------------------------------
 * 【知识点】
 *   类不一定写在类体里，也可以直接定义在某个方法体的内部，这叫局部内部类。
 *   它的“可见范围”被锁死在那个方法里：方法之外谁都看不见它，也用不了它。
 *   局部内部类有个很有用的能力——它能“捕获”所在方法里的局部变量（包括方法
 *   参数），但被捕获的变量必须是“事实最终”的：即赋值之后不再被改变。
 *
 * 【本关任务】
 *   下方静态方法 makeGreeting(String name) 里定义了一个局部类 Greeting，
 *   它有方法 text() 要返回 "你好，" + name。
 *   1. 补全 Greeting.text()，返回 "你好，" + name（这里用到了被捕获的参数 name，
 *      所以 name 在方法里不要再被重新赋值）。
 *   2. 在 makeGreeting 里创建一个 Greeting 对象，并返回它的 text() 结果。
 *   3. 在 main 中调用 makeGreeting("世界") 并打印，得到一行「你好，世界」。
 * ============================================================
 */
public class Ex03_LocalClass {

    static String makeGreeting(String name) {
        class Greeting {
            String text() {
                throw new UnsupportedOperationException("请补全：返回 \"你好，\" + name");
            }
        }
        // TODO(关卡3-1)：创建一个 Greeting 对象，并返回它的 text() 结果
        return "";
    }

    public static void main(String[] args) {
        // TODO(关卡3-2)：调用 makeGreeting("世界") 并打印结果
    }
}

/*
 * ============================ 预期输出 ============================
 * 你好，世界
 * ===============================================================
 *
 * 排查提示：
 *  - 局部类只在它所在的方法里可见；不要试图在 main 里直接 new Greeting。
 *  - 如果编译提示局部变量必须是 final 或事实最终，说明你在方法里改动了 name，
 *    被局部类捕获的变量赋值后就不能再变。
 */
