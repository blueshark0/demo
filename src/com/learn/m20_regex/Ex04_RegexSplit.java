package com.learn.m20_regex;

import java.util.Arrays;

/*
 * ============================================================
 * 模块：m20 正则表达式
 * 关卡：Ex04 用正则 split 切分
 * ------------------------------------------------------------
 * 【知识点】
 *   字符串的切分方法看起来只是“按某个分隔符拆开”，其实它的分隔符参数也是
 *   正则规则。利用这一点，就能把“分隔符两侧那些不固定的空白”一并纳入分隔符，
 *   从而直接切出干净、不带多余空格的片段，省去切完再逐段去空格的麻烦。
 *   描述“任意个空白字符”有专门的元字符，配上“零个或多个”的量词，再把分隔
 *   符号夹在中间即可。源码里反斜杠仍要写两个。
 *
 * 【本关任务】
 *   字符串 "one, two ,three" 里逗号两侧的空格不一致。请用一个正则作为分隔符，
 *   把“逗号及其两侧任意个空白”整体当作分隔符切分，切出干净的三段，再用
 *   Arrays 的数组转字符串方法打印整个数组，结果应为「[one, two, three]」。
 * ============================================================
 */
public class Ex04_RegexSplit {

    public static void main(String[] args) {
        String s = "one, two ,three";
        // TODO(关卡4-1)：用正则分隔符切分 s，把逗号两侧空白一并当分隔符，再打印数组「[one, two, three]」
    }
}

/*
 * ============================ 预期输出 ============================
 * [one, two, three]
 * ===============================================================
 *
 * 排查提示：
 *  - 若某一段前后还带着空格（如「 two 」），说明分隔符没有把两侧空白吃进去。
 *  - 数组要用 Arrays 的转字符串方法打印，直接打印数组对象会得到一串地址。
 */
