package com.learn.m20_regex;

/*
 * ============================================================
 * 模块：m20 正则表达式
 * 关卡：Ex03 replaceAll / replaceFirst
 * ------------------------------------------------------------
 * 【知识点】
 *   字符串提供了两个基于正则的替换方法：一个会把“所有”符合规则的片段都
 *   换成新内容；另一个只换“第一处”符合规则的片段。它们的查找部分都是
 *   正则规则，而不是普通的死板文本。和别的字符串方法一样，它们都不会改动
 *   原字符串，而是返回一个替换后的新字符串。源码里反斜杠仍要写两个。
 *
 * 【本关任务】
 *   对字符串 "a1b2c3"：
 *   1. 用“替换所有”的方法，把其中所有数字都替换成 "#"，打印结果「a#b#c#」。
 *   2. 用“只替换第一处”的方法，只把第一个数字替换成 "#"，打印结果「a#b2c3」。
 * ============================================================
 */
public class Ex03_ReplaceAllFirst {

    public static void main(String[] args) {
        String s = "a1b2c3";

        // TODO(关卡3-1)：把 s 中所有数字替换成 "#"，打印「a#b#c#」

        // TODO(关卡3-2)：只把 s 中第一个数字替换成 "#"，打印「a#b2c3」
    }
}

/*
 * ============================ 预期输出 ============================
 * a#b#c#
 * a#b2c3
 * ===============================================================
 *
 * 排查提示：
 *  - 两行应该不同：第一行所有数字都没了，第二行只有头一个数字变成了 #。
 *  - 若两行一模一样，检查是不是两处用了同一个方法。
 */
