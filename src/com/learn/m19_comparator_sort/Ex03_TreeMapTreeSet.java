package com.learn.m19_comparator_sort;

import java.util.TreeMap;
import java.util.TreeSet;

/*
 * ============================================================
 * 模块：m19 比较器与排序进阶 + 集合补全
 * 关卡：Ex03 TreeMap / TreeSet 自定义排序
 * ------------------------------------------------------------
 * 【知识点】
 *   普通的 Map / Set 不保证元素的先后顺序。而 TreeMap 与 TreeSet 是
 *   “始终保持有序”的容器：你每放进一个元素，它都会自动安插到正确位置。
 *   默认情况下它们按键的自然顺序排列（字符串按字典序、数字按从小到大）。
 *   如果你想换一种顺序（例如从大到小），可以在创建它们时传入一个比较器，
 *   容器就会改用这个比较器来决定先后。
 *
 * 【本关任务】
 *   1. 创建一个 TreeMap，依次放入 banana=1、apple=2、cherry=3，
 *      直接打印它（自然升序会自动整理键的顺序）。
 *      应得「{apple=2, banana=1, cherry=3}」。
 *   2. 创建一个“按从大到小排序”的 TreeSet，依次加入 3、1、2、5、4，
 *      直接打印它。应得「[5, 4, 3, 2, 1]」。
 * ============================================================
 */
public class Ex03_TreeMapTreeSet {

    public static void main(String[] args) {
        // TODO(关卡3-1)：用 TreeMap 放入 banana=1、apple=2、cherry=3 并打印
        //               （应为 {apple=2, banana=1, cherry=3}）

        // TODO(关卡3-2)：用“降序比较器”创建 TreeSet，加入 3、1、2、5、4 并打印
        //               （应为 [5, 4, 3, 2, 1]）
    }
}

/*
 * ============================ 预期输出 ============================
 * {apple=2, banana=1, cherry=3}
 * [5, 4, 3, 2, 1]
 * ===============================================================
 *
 * 排查提示：
 *  - 若 TreeMap 打印出来不是按 a、b、c 的顺序，确认用的是 TreeMap 而非普通 Map。
 *  - 若 TreeSet 打印成升序 [1,2,3,4,5]，说明创建时没传入“降序比较器”。
 */
