package com.learn.m19_comparator_sort;

import java.util.LinkedHashMap;

/*
 * ============================================================
 * 模块：m19 比较器与排序进阶 + 集合补全
 * 关卡：Ex04 LinkedHashMap 保插入序
 * ------------------------------------------------------------
 * 【知识点】
 *   普通的 HashMap 不保证遍历顺序，你放进去的先后和打印出来的先后可能对不上。
 *   LinkedHashMap 则会牢牢记住“插入顺序”：你先放谁、后放谁，遍历和打印时
 *   就还是这个顺序。注意它和上一关 TreeMap 的“按键排序”是两回事——一个是
 *   “按你放入的先后”，一个是“按键本身的大小”，都属于“有序”，但含义不同。
 *
 * 【本关任务】
 *   用 LinkedHashMap 依次放入 one=1、two=2、three=3，直接打印它。
 *   因为它保留插入顺序，结果应与放入顺序一致：「{one=1, two=2, three=3}」。
 * ============================================================
 */
public class Ex04_LinkedHashMapOrder {

    public static void main(String[] args) {
        // TODO(关卡4-1)：用 LinkedHashMap 依次放入 one=1、two=2、three=3 并打印
        //               （应为 {one=1, two=2, three=3}）
    }
}

/*
 * ============================ 预期输出 ============================
 * {one=1, two=2, three=3}
 * ===============================================================
 *
 * 排查提示：
 *  - 若打印顺序变成了 one、three、two 之类，多半用成了普通 HashMap。
 *  - 若结果按键字母排成 one、three、two，那是 TreeMap 的“按键排序”，不是本关要的。
 */
