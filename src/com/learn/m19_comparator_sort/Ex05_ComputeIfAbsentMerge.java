package com.learn.m19_comparator_sort;

import java.util.ArrayList;
import java.util.List;
import java.util.TreeMap;

/*
 * ============================================================
 * 模块：m19 比较器与排序进阶 + 集合补全
 * 关卡：Ex05 computeIfAbsent / merge 简化累积
 * ------------------------------------------------------------
 * 【知识点】
 *   往 Map 里“累积”数据时，传统写法常要先判断键在不在、再分别处理，很啰嗦。
 *   Map 提供了两个便捷方法把这件事一步到位：
 *   - 一个适合“计数累加”：键不存在就放入初始值，键已存在就把旧值和新值
 *     按你给的合并规则合并起来（典型用于词频统计）。
 *   - 另一个适合“分桶”：当某个键还没有对应的容器（如一个空列表）时，才临时
 *     创建并放入，然后你就能往这个容器里继续添加元素（典型用于 Map 套 List）。
 *   本关把结果都装进 TreeMap，借助它“按键排序”让输出稳定可对照。
 *
 * 【本关任务】
 *   1. 对给定序列 a、b、a、c、b、a 做词频统计，用“计数累加”的便捷方法
 *      把每个字母出现次数累加到一个 TreeMap，打印「{a=3, b=2, c=1}」。
 *   2. 把 0 到 5 这六个整数按奇偶分桶：偶数放进键 "even" 对应的列表，奇数放进
 *      键 "odd" 对应的列表，用“分桶”的便捷方法装进一个 TreeMap，打印
 *      「{even=[0, 2, 4], odd=[1, 3, 5]}」。
 * ============================================================
 */
public class Ex05_ComputeIfAbsentMerge {

    public static void main(String[] args) {
        String[] words = {"a", "b", "a", "c", "b", "a"};

        // TODO(关卡5-1)：用“计数累加”的便捷方法统计词频，装入 TreeMap 并打印
        //               （应为 {a=3, b=2, c=1}）

        // TODO(关卡5-2)：用“分桶”的便捷方法把 0..5 按奇偶分到 "even"/"odd"
        //               的列表里，装入 TreeMap 并打印
        //               （应为 {even=[0, 2, 4], odd=[1, 3, 5]}）
    }
}

/*
 * ============================ 预期输出 ============================
 * {a=3, b=2, c=1}
 * {even=[0, 2, 4], odd=[1, 3, 5]}
 * ===============================================================
 *
 * 排查提示：
 *  - 若词频全是 1，说明“键已存在时合并旧值”这一步没生效。
 *  - 若分桶时报空指针，多半是没在“键不存在时”先创建好那个空列表。
 *  - 若键的顺序不是 a、b、c 或 even、odd，确认装结果用的是 TreeMap。
 */
