package com.learn.m03_controlflow;

/*
 * ============================================================
 * 模块：m03 控制流
 * 关卡：Ex01 if / else if / else 分支
 * ------------------------------------------------------------
 * 【知识点】
 *   if 根据条件决定是否执行一段代码。多个条件可以用 else if 串联，
 *   都不满足时走 else：
 *       if (条件A) { ... }
 *       else if (条件B) { ... }
 *       else { ... }
 *   程序会从上往下检查，命中第一个成立的分支后就不再看后面的。
 *
 * 【本关任务】
 *   给 score 评级：>=90 优秀，>=80 良好，>=60 及格，否则 不及格。
 *   这里 score = 75，请补全中间两个条件。
 *
 * 【如何闯关】
 *   把两处占位 false 改成正确的条件，运行对照输出。
 * ============================================================
 */
public class Ex01_IfElse {

    public static void main(String[] args) {
        int score = 75;
        String grade;

        if (score >= 90) {
            grade = "优秀";
        // TODO(关卡1-1)：把 false 改成 score >= 80
        } else if (score >= 80) {
            grade = "良好";
        // TODO(关卡1-2)：把 false 改成 score >= 60
        } else if (score >= 60) {
            grade = "及格";
        } else {
            grade = "不及格";
        }

        System.out.println("分数 " + score + " 的等级是：" + grade);
    }
}

/*
 * ============================ 预期输出 ============================
 * 分数 75 的等级是：及格
 * ===============================================================
 *
 * 排查提示：
 *  - 75 不满足 >=90、也不满足 >=80，但满足 >=60，所以是“及格”。
 *  - 如果你看到“不及格”，说明两个条件还没补对。
 */
