package com.learn.m12_lambda_stream;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

/*
 * ============================================================
 * 模块：m12 Lambda 与 Stream
 * 关卡：Ex04 Stream：filter + map + collect
 * ------------------------------------------------------------
 * 【知识点】
 *   Stream 把对集合的处理写成一条“流水线”：先把集合变成流，中间可以接上
 *   若干步“只描述做什么”的操作——比如按条件“过滤”只留下一部分元素，或把
 *   每个元素“转换”成新值——最后再“收集”成一个结果。整个过程不会改变原集合，
 *   而是产出一个新结果。常用的过滤、转换、收集分别叫 filter、map、collect。
 *
 * 【本关任务】
 *   下方已给定 nums = [1, 2, 3, 4, 5, 6]（保持不变）。请在流水线里补上两步：
 *   先“只保留偶数”，再把保留下来的“每个数乘 2”，最后收集成列表 result，
 *   使程序打印：
 *       偶数翻倍后 = [4, 8, 12]
 * ============================================================
 */
public class Ex04_StreamFilterMap {

    public static void main(String[] args) {
        List<Integer> nums = Arrays.asList(1, 2, 3, 4, 5, 6);

        List<Integer> result = nums.stream()
                // TODO(关卡4-1)：在这里接一步过滤，只保留偶数

                // TODO(关卡4-2)：在这里接一步转换，把保留下来的每个数乘 2

                .collect(Collectors.toList());

        System.out.println("偶数翻倍后 = " + result);
    }
}

/*
 * ============================ 预期输出 ============================
 * 偶数翻倍后 = [4, 8, 12]
 * ===============================================================
 *
 * 排查提示：
 *  - 偶数是 2、4、6，各自翻倍得到 4、8、12。
 *  - 如果输出是 [1, 2, 3, 4, 5, 6]，说明过滤 / 转换这两步还没补上。
 */
