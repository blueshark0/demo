package com.learn.m14_commonapi;

import java.util.Random;

/*
 * ============================================================
 * 模块：m14 常用 API
 * 关卡：Ex04 随机数 Random
 * ------------------------------------------------------------
 * 【知识点】
 *   Random 用来生成随机数，可以让它每次生成一个落在指定上界以内、
 *   从 0 开始（含 0、不含上界）的随机整数。它内部有一个“种子”，种子
 *   决定整条随机序列：相同的种子会生成完全相同的序列。本练习用固定
 *   种子创建 Random，所以每次运行结果都一样，方便对照预期输出。
 *   （真实项目通常用无参方式创建 Random，每次结果都不一样。）
 *
 * 【本关任务】
 *   下方已用固定种子 42 创建好 random（请勿改动这行）。在循环里连续
 *   取 3 个 [0, 100) 的随机整数并逐行打印，使输出为：
 *       30
 *       63
 *       48
 * ============================================================
 */
public class Ex04_Random {

    public static void main(String[] args) {
        Random random = new Random(42); // 固定种子，保证结果可预期

        for (int i = 0; i < 3; i++) {
            // TODO(关卡4-1)：用 random 取一个 [0, 100) 的随机整数并打印（依次应为 30、63、48）
        }
    }
}

/*
 * ============================ 预期输出 ============================
 * 30
 * 63
 * 48
 * ===============================================================
 *
 * 排查提示：
 *  - 因为种子固定是 42，这三个数在任何电脑、任何 JDK 上都应该是 30、63、48。
 *  - 如果你把种子 42 改成别的数字，序列就会不同，可以试试看。
 */
