package com.learn.m18_stream_collectors;

import java.util.IntSummaryStatistics;

/*
 * ============================================================
 * 模块：m18 Stream 进阶 / Collectors
 * 关卡：Ex05 IntStream 汇总统计
 * ------------------------------------------------------------
 * 【知识点】
 *   普通的 Stream<Integer> 处理数字时会有装箱拆箱的开销，也没有现成的求和、
 *   求平均方法。IntStream 是专门处理 int 的“数值流”，自带 sum / average /
 *   max / min 等数值方法。更省事的是 summaryStatistics：它一次遍历就把
 *   总和、平均、最大、最小、个数全算出来，打包成一个统计结果对象，按需取用。
 *   另外 rangeClosed 能直接生成一段“含两端点”的连续整数区间，省去手写循环。
 *
 * 【本关任务】
 *   用 rangeClosed 生成 1 到 5（含 1 和 5）的连续整数，求出它们的汇总统计，
 *   然后按下面四行的确切格式分别打印总和、平均、最大、最小：
 *       总和 = 15
 *       平均 = 3.0
 *       最大 = 5
 *       最小 = 1
 * ============================================================
 */
public class Ex05_IntStreamSummary {

    public static void main(String[] args) {
        // TODO(关卡5-1)：用 rangeClosed 生成 1..5 并求汇总统计，赋给下面的 stats
        IntSummaryStatistics stats = null;

        // TODO(关卡5-2)：从 stats 取出总和，按「总和 = 15」格式打印

        // TODO(关卡5-3)：从 stats 取出平均，按「平均 = 3.0」格式打印

        // TODO(关卡5-4)：从 stats 取出最大，按「最大 = 5」格式打印

        // TODO(关卡5-5)：从 stats 取出最小，按「最小 = 1」格式打印
    }
}

/*
 * ============================ 预期输出 ============================
 * 总和 = 15
 * 平均 = 3.0
 * 最大 = 5
 * 最小 = 1
 * ===============================================================
 *
 * 排查提示：
 *  - 1+2+3+4+5 = 15，平均 15/5 = 3.0（平均值带小数点）。
 *  - 区间要“含两端”：用 rangeClosed 才会包含 5；普通的 range 不含上界。
 *  - 若程序抛出空指针，说明 stats 还没真正算出汇总统计就被取用了。
 */
