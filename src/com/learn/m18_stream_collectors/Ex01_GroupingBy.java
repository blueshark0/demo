package com.learn.m18_stream_collectors;

import java.util.Arrays;
import java.util.List;
import java.util.Map;

/*
 * ============================================================
 * 模块：m18 Stream 进阶 / Collectors
 * 关卡：Ex01 按条件分组 groupingBy
 * ------------------------------------------------------------
 * 【知识点】
 *   groupingBy 是一类“收集器”：它按你给出的“分类键”把流里的元素分到不同的
 *   桶里，最终汇成一个“键 → 该桶里所有元素的列表”的 Map。这比自己写循环往
 *   多个列表里塞元素优雅得多。要注意：默认得到的 Map 不保证任何顺序，所以当
 *   你要“把整个分组结果原样打印出来”时，输出顺序是不确定的；若想稳定、可复现，
 *   就得让收集器把结果装进一个“会按键排序的有序 Map”里再打印。
 *
 * 【本关任务】
 *   下方已给定 nums = [1, 2, 3, 4, 5, 6]（用 Arrays.asList(1,2,3,4,5,6)，保持不变）。
 *   把它们按奇偶分组：偶数归到键「偶数」，奇数归到键「奇数」；并且要求结果
 *   按键升序、可复现，因此分组结果必须装入一个会按键排序的有序 Map（TreeMap）。
 *   把整个 Map 直接打印，使程序输出：
 *       {偶数=[2, 4, 6], 奇数=[1, 3, 5]}
 * ============================================================
 */
public class Ex01_GroupingBy {

    public static void main(String[] args) {
        List<Integer> nums = Arrays.asList(1, 2, 3, 4, 5, 6);

        // TODO(关卡1-1)：把 nums 变成流，按奇偶分组（键用「偶数」/「奇数」），
        //               并让分组结果装入按键升序的有序 Map，赋给下面的 grouped
        Map<String, List<Integer>> grouped = null;

        System.out.println(grouped);
    }
}

/*
 * ============================ 预期输出 ============================
 * {偶数=[2, 4, 6], 奇数=[1, 3, 5]}
 * ===============================================================
 *
 * 排查提示：
 *  - 如果打印出 null，说明 grouped 还没真正被分组结果赋值。
 *  - 如果两组顺序时有时无、和预期不一致，多半是没把结果装进“按键排序”的有序 Map。
 *  - 判断奇偶：一个数除以 2 的余数为 0 就是偶数，否则是奇数。
 */
