package com.learn.m04_arrays;

/*
 * ============================================================
 * 模块：m04 数组
 * 关卡：Ex02 遍历数组求和
 * ------------------------------------------------------------
 * 【知识点】
 *   遍历就是把数组里的每个元素都访问一遍。常见有两种写法：一种是
 *   普通 for，用一个从 0 递增到“长度减一”的下标变量逐个取元素，
 *   好处是过程中能拿到下标；另一种是增强 for（也叫 for-each），
 *   它直接把元素值依次交给你，写起来更简洁，但拿不到下标。需要下标
 *   时用普通 for，只关心元素值时用增强 for。
 *
 * 【本关任务】
 *   下方给定数组 data = {3, 7, 2, 8, 5}（请保持不变）。
 *   请分别用普通 for 和增强 for 把所有元素累加起来，使程序打印：
 *       普通 for 求和 = 25
 *       增强 for 求和 = 25
 * ============================================================
 */
public class Ex02_Traverse {

    public static void main(String[] args) {
        int[] data = {3, 7, 2, 8, 5};

        int sum1 = 0;
        for (int i = 0; i < data.length; i++) {
            // TODO(关卡2-1)：把当前下标对应的元素累加到 sum1


        }

        int sum2 = 0;
        for (int v : data) {
            // TODO(关卡2-2)：把当前元素 v 累加到 sum2


        }

        System.out.println("普通 for 求和 = " + sum1);
        System.out.println("增强 for 求和 = " + sum2);
    }
}

/*
 * ============================ 预期输出 ============================
 * 普通 for 求和 = 25
 * 增强 for 求和 = 25
 * ===============================================================
 *
 * 排查提示：
 *  - 3+7+2+8+5 = 25，两种写法结果应当一致。
 *  - 增强 for 写起来更简洁，但拿不到下标 i；需要下标时用普通 for。
 */
