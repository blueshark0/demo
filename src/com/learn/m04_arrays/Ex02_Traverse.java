package com.learn.m04_arrays;

/*
 * ============================================================
 * 模块：m04 数组
 * 关卡：Ex02 遍历数组求和
 * ------------------------------------------------------------
 * 【知识点】
 *   遍历 = 把数组里每个元素都访问一遍。两种常用写法：
 *     普通 for（能拿到下标 i）：
 *         for (int i = 0; i < data.length; i++) { ... data[i] ... }
 *     增强 for / for-each（直接拿到元素值，更简洁）：
 *         for (int v : data) { ... v ... }
 *
 * 【本关任务】
 *   分别用两种 for 把数组所有元素加起来。
 *
 * 【如何闯关】
 *   在两个循环体里各补一行累加语句，运行对照输出。
 * ============================================================
 */
public class Ex02_Traverse {

    public static void main(String[] args) {
        int[] data = {3, 7, 2, 8, 5};

        int sum1 = 0;
        for (int i = 0; i < data.length; i++) {
            // TODO(关卡2-1)：把 data[i] 累加到 sum1
            //   提示：sum1 += data[i];
        }

        int sum2 = 0;
        for (int v : data) {
            // TODO(关卡2-2)：把 v 累加到 sum2
            //   提示：sum2 += v;
        }

        System.out.println("普通 for 求和 = " + sum1);
        System.out.println("增强 for 求和 = " + sum2);
    }
}

/*
 * ============================ 预期输出 ============================
 * 普通 for 求和 = 25
 * 增强 for 求和 = 25
 * ===============================================================
 *
 * 排查提示：
 *  - 3+7+2+8+5 = 25，两种写法结果应当一致。
 *  - 增强 for 写起来更简洁，但拿不到下标 i；需要下标时用普通 for。
 */
