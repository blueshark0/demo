package com.learn.m04_arrays;

/*
 * ============================================================
 * 模块：m04 数组
 * 关卡：Ex01 创建数组与下标访问
 * ------------------------------------------------------------
 * 【知识点】
 *   创建一个能装 3 个 int 的数组： int[] nums = new int[3];
 *   刚创建时元素默认都是 0。用“下标”读写元素，下标从 0 开始：
 *       nums[0] = 10;            // 给第 1 个元素赋值
 *       int x = nums[0];         // 读取第 1 个元素
 *   nums.length 得到数组长度（这里是 3），最大合法下标是 length-1。
 *
 * 【本关任务】
 *   给 nums 的三个元素分别赋值 10、20、30。
 *
 * 【如何闯关】
 *   补全三行赋值语句，运行对照输出。
 * ============================================================
 */
public class Ex01_CreateAndAccess {

    public static void main(String[] args) {
        int[] nums = new int[3];

        // TODO(关卡1-1)：给 nums[0] 赋值 10
        // TODO(关卡1-2)：给 nums[1] 赋值 20
        // TODO(关卡1-3)：给 nums[2] 赋值 30
        //   提示：写法形如  nums[0] = 10;

        System.out.println("长度 = " + nums.length);
        System.out.println("nums[0] = " + nums[0]);
        System.out.println("nums[1] = " + nums[1]);
        System.out.println("nums[2] = " + nums[2]);
    }
}

/*
 * ============================ 预期输出 ============================
 * 长度 = 3
 * nums[0] = 10
 * nums[1] = 20
 * nums[2] = 30
 * ===============================================================
 *
 * 排查提示：
 *  - 如果元素都是 0，说明赋值语句还没补全。
 *  - 试试访问 nums[3]，你会看到“下标越界”异常，因为合法下标只有 0~2。
 */
