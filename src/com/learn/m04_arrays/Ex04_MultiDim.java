package com.learn.m04_arrays;

/*
 * ============================================================
 * 模块：m04 数组
 * 关卡：Ex04 二维数组
 * ------------------------------------------------------------
 * 【知识点】
 *   二维数组可以理解为“表格”（行 × 列），本质是“数组的数组”。它有
 *   多少“行”，每一行又各自是一个一维数组，里面有多少“列”。访问某个
 *   格子需要两个下标：先选第几行，再选这一行里的第几列。遍历整张表格
 *   要用双重循环：外层循环逐行走，内层循环在当前行里逐列走。
 *
 * 【本关任务】
 *   下方给定矩阵 matrix = {{1, 2, 3}, {4, 5, 6}}（请保持不变）。
 *   请按“行”把它打印出来：同一行的元素之间用一个空格分隔，每行打印
 *   完后换行。使程序打印：
 *       1 2 3
 *       4 5 6
 * ============================================================
 */
public class Ex04_MultiDim {

    public static void main(String[] args) {
        int[][] matrix = {
                {1, 2, 3},
                {4, 5, 6}
        };

        for (int r = 0; r < matrix.length; r++) {
            for (int c = 0; c < matrix[r].length; c++) {
                // TODO(关卡4-1)：打印当前格子的元素，后面再跟一个空格，且不要换行


            }
            System.out.println(); // 每行结束后换行
        }
    }
}

/*
 * ============================ 预期输出 ============================
 * 1 2 3
 * 4 5 6
 * ===============================================================
 *
 * 排查提示：
 *  - 每行末尾其实有一个空格（肉眼看不出来），属于正常现象。
 *  - 如果所有数字挤在一行，是因为内层循环里用了 println；应该用 print，
 *    换行交给外层循环后面的 println 来做。
 */
