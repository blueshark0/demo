package com.learn.m21_concurrency;

import java.util.concurrent.*;

/*
 * ============================================================
 * 模块：m21 多线程入门
 * 关卡：Ex05 线程池 submit/Future/shutdown
 * ------------------------------------------------------------
 * 【知识点】
 *   前几关每个任务都手动 new 一个线程，任务一多就既浪费又难管理。线程池是一组提前备好、
 *   可反复复用的线程：你只管把任务“提交”进去，由池子调度空闲线程来执行。如果任务需要
 *   返回一个结果，提交后会立刻拿到一个“凭据”，将来用它去取真正的结果（取的时候若还没算完
 *   会自动等待）。线程池用完必须关闭，否则程序里非守护线程不退出、main 跑完也不结束；
 *   关闭后通常还要再等一小会儿，确认在跑的任务都收尾。
 *
 * 【本关任务】
 *   1. 建一个固定大小为 4 的线程池。
 *   2. 提交 4 个有返回值的任务，第 i 个（i 从 1 到 4）返回 i*i。
 *      （注意：Lambda 捕获循环变量需要一个 final 的副本，如 final int n = i。）
 *   3. 主线程依次取回 4 个任务的结果并求和，打印「线程池计算平方和 = 30」（1+4+9+16）。
 *   4. 关闭线程池，并最多等待 1 秒让在跑的任务收尾。
 * ============================================================
 */
public class Ex05_ExecutorServicePool {

    public static void main(String[] args) throws Exception {
        // TODO(关卡5-1)：建一个固定大小为 4 的线程池

        // TODO(关卡5-2)：提交 4 个任务，第 i 个（i 从 1 到 4）返回 i*i，保存好各自的“取结果凭据”

        // TODO(关卡5-3)：依次取回 4 个结果求和，打印「线程池计算平方和 = 30」

        // TODO(关卡5-4)：关闭线程池，并最多等待 1 秒让在跑的任务收尾
    }
}

/*
 * ===================== 预期输出（示例，顺序可能不同） =====================
 * 说明：多线程执行顺序每次运行可能不同，以下仅为示例；
 *       过关判据是「最终聚合结果」那一行（恒定值），无需逐行严格对照。
 * 线程池计算平方和 = 30
 * =====================================================================
 *
 * 排查提示：
 *  - 如果程序打印完结果却迟迟不结束，多半是忘了关闭线程池。
 *  - 取结果时若还没算完会自动等待，所以求和拿到的一定是完整的 30。
 */
