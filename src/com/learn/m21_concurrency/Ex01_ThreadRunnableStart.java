package com.learn.m21_concurrency;

/*
 * ============================================================
 * 模块：m21 多线程入门
 * 关卡：Ex01 用 Runnable+Lambda 创建、start、join
 * ------------------------------------------------------------
 * 【知识点】
 *   线程是程序里能“同时”往前走的一条独立执行流。要让一段代码在新线程里跑，
 *   先用一个任务对象描述“要干的活”（这种任务用 Lambda 写最省事），再把它交给
 *   一个线程去启动。注意：真正开启异步执行的是“启动”动作，而不是创建对象本身。
 *   线程一旦启动，它和主线程就各跑各的、谁先谁后不保证；如果主线程需要等子线程
 *   把活干完再继续（比如要用它算出的结果），就得显式“等待汇合”，否则主线程可能
 *   在子线程还没算完时就读到了半成品。
 *
 * 【本关任务】
 *   已为你准备好共享容器 int[] result = {0}（用数组是为了让 Lambda 能修改其中的值）。
 *   1. 创建并启动一个子线程，让它把 1..100 累加进 result[0]。
 *   2. 让主线程等子线程干完，再打印「子线程计算 1..100 之和 = 5050」。
 * ============================================================
 */
public class Ex01_ThreadRunnableStart {

    public static void main(String[] args) throws Exception {
        int[] result = {0};

        // TODO(关卡1-1)：创建一个子线程，让它把 1..100 累加进 result[0]，并启动它

        // TODO(关卡1-2)：让主线程等待子线程干完，再打印「子线程计算 1..100 之和 = 5050」
    }
}

/*
 * ===================== 预期输出（示例，顺序可能不同） =====================
 * 说明：多线程执行顺序每次运行可能不同，以下仅为示例；
 *       过关判据是「最终聚合结果」那一行（恒定值），无需逐行严格对照。
 * 子线程计算 1..100 之和 = 5050
 * =====================================================================
 *
 * 排查提示：
 *  - 如果打印出来是 0 或一个偏小的数，多半是主线程没等子线程干完就读了 result[0]。
 *  - 如果数值随机变化，检查是否真的“等待汇合”之后才打印。
 */
